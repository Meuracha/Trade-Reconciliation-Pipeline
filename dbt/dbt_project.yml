name: trade_recon
version: "1.0.0"
config-version: 2

profile: trade_recon

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  trade_recon:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    mart:
      +materialized: table